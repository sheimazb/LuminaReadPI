import { Box, Button, Flex, Text } from "@chakra-ui/react";

const AddPackage = () => {
    return (
        <Box>
            <Flex>Add package</Flex>
        </Box>
    );
};

export default AddPackage;
